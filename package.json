{
  "name": "dns-lookup-sync",
  "author": "Khiet Tam Nguyen",
  "license": "MIT",
  "description": "Synchronous version of dnsPromises.lookup -  Resolves a host name (e.g. 'nodejs.org') into the first found A (IPv4) or AAAA (IPv6) record",
  "repository": {
    "type": "git",
    "url": "https://github.com/nktnet1/dns-lookup-sync"
  },
  "version": "1.0.0",
  "files": [
    "dist",
    "scripts"
  ],
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "tc": "jest --coverage",
    "lint": "eslint './**/*.ts'",
    "lf": "eslint --fix './**/*.ts'",
    "tsc": "tsc --noEmit",
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "dns",
    "sync",
    "synchronous",
    "lookup",
    "dns-lookup",
    "dns-sync",
    "domain",
    "resolve",
    "IPv4",
    "IPv6",
    "comp1531"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.2",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.0",
    "eslint-plugin-jest": "^27.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
